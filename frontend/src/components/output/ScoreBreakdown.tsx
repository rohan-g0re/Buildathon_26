/**
 * ScoreBreakdown — 4-metric radar/bar chart per move.
 * Shows Impact, Feasibility, Risk-Adjusted Return, Strategic Alignment.
 *
 * See: docs/architecture/LLD_frontend.md § 9
 */

"use client";

// TODO: Implement ScoreBreakdown
// - Use Recharts RadarChart or BarChart
// - 4 metrics per agent (D1, D2, D3)
// - Color-coded by agent
// - Compact display that fits inside MoveCard

export function ScoreBreakdown() {
  return (
    <div className="mt-4">
      <p className="text-zinc-500 text-xs">ScoreBreakdown — TODO</p>
    </div>
  );
}
